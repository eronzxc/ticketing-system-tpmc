<?php
// ===== EXAMPLE FILE — copy this, don't edit this one directly =====
//
// 1. Duplicate this file in the same folder and rename the copy to:
//        db_secrets.php
// 2. Fill in your real DB host/name/username/password below (in the
//    COPY, not here).
// 3. db_secrets.php is already listed in .gitignore, so it will never
//    be committed/pushed — only db_secrets.example.php (this template)
//    goes to GitHub.
//
// Default XAMPP values (localhost, no password) are filled in below
// as a starting point — safe to leave as-is for local development,
// but should be changed to a real username/password before this is
// ever run on a shared/hospital network.
define('DB_HOST', 'localhost');
define('DB_NAME', 'tpmc_ticketing');
define('DB_USER', 'root');
define('DB_PASS', '');
