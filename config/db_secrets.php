<?php
// This is YOUR real DB credentials file — safe to edit freely.
// Listed in .gitignore, so it will never be committed/pushed to GitHub;
// only db_secrets.example.php (the template) goes there.
//
// Current values = default XAMPP setup (localhost, root, no password).
// Change DB_USER/DB_PASS here if you set up a real MySQL account,
// e.g. before deploying to the hospital LAN.
define('DB_HOST', 'localhost');
define('DB_NAME', 'tpmc_ticketing');
define('DB_USER', 'root');
define('DB_PASS', '');
